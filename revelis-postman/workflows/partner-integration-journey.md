# Workflow: Partner Integration Journey
Enables third-party systems to scan and validate tickets via `POST /issued-tickets/validate` and record gate entries (`POST /issued-tickets/:ticketNumber/check-in`).
